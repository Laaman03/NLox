class LoxClass {
  init(foo, bar) {
    this.foo = foo;
    this.bar = bar;
  }

  sayFoo() { print this.foo; }
  sayBar() { print this.bar; }
}

var a = LoxClass("hello", "goobye");

a.sayFoo();
a.sayBar();